# 待修复问题汇总

记录用户反馈的问题与修复进度。请在每次反馈和修复后更新。本文件仅以用户确认“已修复”为最终状态。

## 2025-10-31

- 问题：H5 预览报错，浏览器显示 `net::ERR_ABORTED /src/App.vue`，控制台覆盖层提示 `[plugin:vite:vue] At least one <template> or <script> is required`。
  - 影响范围：页面无法正常加载，无法预览新增迁移页面。
  - 初步结论：可能存在构建插件冲突或资源缺失。
  - 修复方案（已实施，待用户确认）：
    1. 移除 `vite.config.js` 中的 `@vitejs/plugin-vue`，仅保留 `@dcloudio/vite-plugin-uni`；
    2. 将 `src/App.vue` 简化为标准 SFC；
    3. 移除 `src/pages/login/index.vue` 对缺失图片 `@/assets/images/comwechat.png` 的引用，改为占位符；
    4. 重启开发服务器并验证首页与知识库、营销话术、产品图册页面加载正常。
  - 待用户确认：问题是否已消除，页面是否可正常访问。

> 说明：若用户确认已修复，将在此处追加“已修复”条目并记录具体时间与验证结果。