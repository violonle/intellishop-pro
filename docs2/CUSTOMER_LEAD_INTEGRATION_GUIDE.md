# 客户和线索管理前后端集成指南\n\n## 概述\n\n本文档说明如何在前端页面中集成后端API，实现客户和线索管理的完整功能。\n\n## 第4项集成任务：客户/线索管理页面集成\n\n### 包含内容\n\n1. **客户管理页面** (`pages/customer-list.html`, `pages/customer-detail.html`)\n2. **线索管理页面** (`pages/leads.html`, `pages/lead-detail.html`)\n3. **API客户端** (`assets/js/api-client.js` - 已完成，第1项任务)\n4. **集成脚本** (`assets/js/customer-lead-integration.js` - 本次新增)\n\n## 集成脚本功能\n\n### 1. 客户管理 API\n\n#### 加载客户列表\n```javascript\nconst result = await loadCustomers(filter, keyword, page);\n// filter: 'all'(全部) | 'vip'(VIP) | 'high-value'(高价值) | 'active'(活跃) | 'dormant'(沉睡)\n// keyword: 搜索关键词\n// page: 页码\n\n// 返回:\n// {\n//   customers: [...],  // 客户列表数组\n//   total: 50,         // 总数\n//   pageNo: 1,\n//   pageSize: 10\n// }\n```\n\n#### 获取客户详情\n```javascript\nconst customer = await getCustomerDetail(customerId);\n// 返回单个客户对象的完整信息\n```\n\n#### 创建客户\n```javascript\nconst newCustomer = await createCustomer({\n    realName: '张三',\n    phone: '13888888888',\n    email: 'zhangsan@example.com',\n    company: 'ABC公司',\n    position: '经理',\n    address: '北京朝阳',\n    // ... 其他字段\n});\n```\n\n#### 更新客户\n```javascript\nconst updated = await updateCustomer(customerId, {\n    realName: '新名字',\n    status: 'active',\n    // ... 需要更新的字段\n});\n```\n\n#### 删除客户\n```javascript\nconst success = await deleteCustomer(customerId);\n// 会显示确认对话框\n```\n\n#### 分配客户\n```javascript\nconst assigned = await assignCustomer(customerId, assignedUserId);\n```\n\n### 2. 线索管理 API\n\n#### 加载线索列表\n```javascript\nconst result = await loadLeads(filter, keyword, page);\n// filter: 'all' | 'new' | 'contacted' | 'qualified' | 'proposal' | 'negotiation' | 'won' | 'lost'\n```\n\n#### 获取线索详情\n```javascript\nconst lead = await getLeadDetail(leadId);\n```\n\n#### 创建线索\n```javascript\nconst newLead = await createLead({\n    title: '线索标题',\n    description: '线索描述',\n    source: '官网咨询',\n    priority: 'high',  // urgent | high | medium | low\n    estimatedValue: 5000,\n    // ... 其他字段\n});\n```\n\n#### 更新线索\n```javascript\nconst updated = await updateLead(leadId, {\n    status: 'qualified',\n    // ... 需要更新的字段\n});\n```\n\n#### 线索转客户\n```javascript\nconst customer = await convertLeadToCustomer(leadId, {\n    realName: '客户名字',\n    phone: '联系电话',\n    // ... 客户其他信息\n});\n```\n\n### 3. 辅助函数\n\n```javascript\n// 隐藏电话号码 (138****8888)\nmaskPhone(phone);\n\n// 格式化日期 (今天 | 1天前 | 2周前等)\nformatDate(dateString);\n\n// 获取客户等级标签\ngetLevelLabel(level);  // 'diamond' => '钻石', 'vip' => 'VIP'\n\n// 获取客户状态标签\ngetStatusLabel(status);  // 'active' => '活跃'\n\n// 获取线索优先级标签\ngetPriorityLabel(priority);  // 'high' => '高'\n\n// 获取线索状态标签  \ngetLeadStatusLabel(status);  // 'won' => '已成交'\n```\n\n## 页面集成步骤\n\n### 第1步：在HTML页面中引入脚本\n\n在 `<head>` 标签或 `</body>` 前添加：\n\n```html\n<!-- 必需的全局脚本 -->\n<script src=\"../assets/js/utils.js\"></script>\n<script src=\"../assets/js/ui-components.js\"></script>\n<script src=\"../assets/js/api-client.js\"></script>\n\n<!-- 客户和线索集成脚本 -->\n<script src=\"../assets/js/customer-lead-integration.js\"></script>\n```\n\n### 第2步：在页面初始化时加载数据\n\n```javascript\n// 页面加载完成后初始化\ndocument.addEventListener('DOMContentLoaded', async () => {\n    // 加载客户列表\n    const result = await loadCustomers('all', '', 1);\n    if (result) {\n        renderCustomerList(result.customers);\n    }\n});\n```\n\n### 第3步：实现列表渲染函数\n\n```javascript\nfunction renderCustomerList(customers) {\n    const container = document.querySelector('.customer-list-container');\n    \n    customers.forEach(customer => {\n        const html = `\n            <div class=\"customer-item\">\n                <h3>${customer.realName}</h3>\n                <p>${maskPhone(customer.phone)}</p>\n                <span class=\"level-badge ${getLevelBgClass(customer.level)}\">\n                    ${getLevelLabel(customer.level)}\n                </span>\n                <p class=\"update-time\">${formatDate(customer.updatedAt)}</p>\n                <button onclick=\"viewCustomerDetail(${customer.id})\">查看详情</button>\n            </div>\n        `;\n        container.insertAdjacentHTML('beforeend', html);\n    });\n}\n```\n\n### 第4步：实现详情页面\n\n```javascript\nasync function viewCustomerDetail(customerId) {\n    const customer = await getCustomerDetail(customerId);\n    \n    // 填充详情页面内容\n    document.getElementById('customer-name').textContent = customer.realName;\n    document.getElementById('customer-phone').textContent = maskPhone(customer.phone);\n    document.getElementById('customer-company').textContent = customer.company;\n    // ... 其他字段\n}\n```\n\n### 第5步：实现编辑/创建功能\n\n```javascript\nasync function saveCustomer(formData) {\n    if (formData.id) {\n        // 更新现有客户\n        await updateCustomer(formData.id, formData);\n    } else {\n        // 创建新客户\n        await createCustomer(formData);\n    }\n    // 重新加载列表\n    const result = await loadCustomers();\n    renderCustomerList(result.customers);\n}\n```\n\n## 后端API返回格式\n\n### 客户对象结构\n```json\n{\n    \"id\": 1,\n    \"realName\": \"张三\",\n    \"phone\": \"13888888888\",\n    \"email\": \"zhangsan@example.com\",\n    \"company\": \"ABC公司\",\n    \"position\": \"经理\",\n    \"address\": \"北京朝阳\",\n    \"level\": \"vip\",  // normal | vip | diamond\n    \"status\": \"active\",  // active | inactive | potential | lost\n    \"tags\": [\"标签1\", \"标签2\"],\n    \"assignedTo\": 2,\n    \"assignedToName\": \"李销售\",\n    \"createdAt\": \"2024-01-15T10:00:00\",\n    \"updatedAt\": \"2024-01-18T15:30:00\"\n}\n```\n\n### 线索对象结构\n```json\n{\n    \"id\": 1,\n    \"title\": \"咨询宝马X5\",\n    \"description\": \"客户咨询宝马X5相关信息\",\n    \"source\": \"官网咨询\",\n    \"priority\": \"high\",  // urgent | high | medium | low\n    \"status\": \"new\",  // new | contacted | qualified | proposal | negotiation | won | lost\n    \"estimatedValue\": 500000,\n    \"successProbability\": 85,\n    \"customerId\": 1,\n    \"assignedTo\": 2,\n    \"createdAt\": \"2024-01-15T10:00:00\",\n    \"updatedAt\": \"2024-01-18T15:30:00\"\n}\n```\n\n## 错误处理\n\n所有API函数都有内置的错误处理和用户提示：\n\n- 加载成功：显示成功消息（可选）\n- 加载失败：自动显示红色错误提示\n- 网络超时：自动提示重试\n- 权限不足：自动重定向到登录页面\n\n## 分页处理\n\n```javascript\nlet currentPage = 1;\nconst pageSize = 10;\n\n// 加载下一页\nasync function nextPage() {\n    currentPage++;\n    const result = await loadCustomers('all', '', currentPage);\n    // 追加到列表或替换列表\n}\n\n// 加载上一页\nasync function prevPage() {\n    if (currentPage > 1) {\n        currentPage--;\n        const result = await loadCustomers('all', '', currentPage);\n    }\n}\n```\n\n## 搜索和筛选\n\n```javascript\n// 搜索客户\nasync function searchCustomers(keyword) {\n    const result = await loadCustomers('all', keyword, 1);\n    renderCustomerList(result.customers);\n}\n\n// 按等级筛选\nasync function filterByLevel(level) {\n    const result = await loadCustomers(level === 'vip' ? 'vip' : 'all', '', 1);\n    renderCustomerList(result.customers);\n}\n```\n\n## 常见问题\n\n### Q: 如何在线索详情页面获取并显示AI分析结果？\n\nA: 在详情页面加载后，调用AI分析API：\n\n```javascript\nconst analysis = await window.api.aiCustomer.analyze(leadId);\n// 显示分析结果\ndocument.getElementById('ai-analysis').innerHTML = \n    `<div>${analysis.prediction}</div>`;\n```\n\n### Q: 如何实现线索转客户的功能？\n\nA: 调用转化函数：\n\n```javascript\nconst customer = await convertLeadToCustomer(leadId, {\n    realName: '客户名字',\n    phone: '联系电话',\n    company: '公司名称'\n});\n\nif (customer) {\n    // 转化成功，重定向到客户详情\n    window.location.href = `customer-detail.html?id=${customer.id}`;\n}\n```\n\n### Q: 如何处理页面刷新时保持搜索和筛选条件？\n\nA: 使用URL参数或sessionStorage：\n\n```javascript\n// 保存搜索条件到sessionStorage\nsessionStorage.setItem('customerFilter', currentFilter);\nsessionStorage.setItem('customerKeyword', searchKeyword);\nsessionStorage.setItem('customerPage', currentPage);\n\n// 页面加载时恢复\nconst savedFilter = sessionStorage.getItem('customerFilter') || 'all';\nconst savedKeyword = sessionStorage.getItem('customerKeyword') || '';\nconst savedPage = parseInt(sessionStorage.getItem('customerPage')) || 1;\n\nloadCustomers(savedFilter, savedKeyword, savedPage);\n```\n\n## 后续集成任务\n\n- **第5项**：产品管理页面集成\n- **第6项**：数据分析仪表板集成\n- **第7项**：AI功能集成（已部分完成）\n- **第8项**：权限控制集成\n- **第9项**：第三方服务集成\n- **第10项**：全面测试与验证\n\n## 支持\n\n如有问题，请参考：\n- API客户端文档：`api-client.js` 中的JSDoc注释\n- UI组件文档：`ui-components.js`\n- 示例页面：`pages/customer-list.html`, `pages/lead-detail.html`\n"